apiVersion: v1
kind: Service
metadata:
  name: user-feedback-service
spec:
  selector:
    app: user-feedback-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 50053
  type: LoadBalancer
